<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to(image_tag("logo.png"), root_url, :class => 'brand') %>
      <div class="nav-collapse collapse">
        <ul class="nav pull-right">
          <li><%= link_to(t('.help'), root_url) %></li>
          <li>
            <% if user_signed_in? %>
                <%= link_to(t('.myaccount'), root_url) %>
            <% else %>
                <%= link_to(t('.sign_in'), '#sign_in', :role => 'button', 'data-toggle' => 'modal', :class=>'visible-desktop') %>
                <%= link_to(t('.sign_in'), new_user_session_path, :class=>'hidden-desktop') %>
            <% end%>
          </li>
          <li><% if user_signed_in? %>
                <%= link_to(t('.logout'), destroy_user_session_path, :method => :delete) %>
            <% else %>
                <%= link_to(t('.sign_up'), '#sign_up', :role => 'button', 'data-toggle' => 'modal', :class=>'visible-desktop') %>
                <%= link_to(t('.sign_up'), new_user_registration_path, :class=>'hidden-desktop') %>
            <% end%>
          </li>
        </ul>
        <ul class="nav lang">
          <li><%= link_to("DE", root_url) %></li>
          <li><%= link_to("FR", root_url) %></li>
          <li><%= link_to("EN", root_url) %></li>
        </ul>
      </div>
    </div>
  </div>
</div>